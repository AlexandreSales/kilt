unit kilt.dlgs.types;

interface

type
  tmsgShow = (tmsAll, tmsMessage);
  tmsgType = (tmCancelar, tmConfirmar, tmErro, tmInformacao, tmInformacaoImportante, tmOk, tmPergunta, tmPerguntaImportante, tmDeletaArquivo, tmMulti);
  tstyleMsg  = (smDefault, smBlack);

implementation

end.
